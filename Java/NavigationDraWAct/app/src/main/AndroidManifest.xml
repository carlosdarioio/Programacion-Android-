<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navigationdrawact">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".TransVer"></activity>
        <activity android:name=".TransListar" />
        <activity android:name=".TransBuscar2" />
        <activity android:name=".CLver" />
        <activity android:name=".ClListar" />
        <activity android:name=".ClBuscar" />
        <activity android:name=".DesbloquearArticulos" />
        <activity android:name=".DarDescuentoManual" />
        <activity android:name=".VerFactura" />
        <activity android:name=".ListarFacturas" />
        <activity android:name=".BuscarFactura" />
        <activity
            android:name=".Main2ActivityBasic"
            android:label="@string/title_activity_main2_basic"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>